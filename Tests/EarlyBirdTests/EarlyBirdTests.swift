import XCTest
@testable import EarlyBird

final class EarlyBirdTests: XCTestCase {
    func testAppScaffoldCompiles() {
        // Placeholder: confirms the test target links against the app target.
        // Replaced by real coverage starting with the AlarmSession model (step 2).
        XCTAssertTrue(true)
    }
}
